/**
 * 图标映射 — 统一使用 lucide-react SVG 图标替代 emoji
 *
 * 所有图标使用 currentColor，通过 CSS color 控制为黑色/灰色。
 * 激活状态通过 className 控制，图标本身不包含颜色。
 */

import React from "react";
import type { LucideIcon } from "lucide-react";
import {
  BarChart3,
  MessageSquare,
  Brain,
  Settings,
  User,
  Bot,
  Play,
  ChevronDown,
  ChevronLeft,
  ChevronRight,
  Clock,
  RefreshCw,
  CheckCircle,
  XCircle,
  BookOpen,
  PenLine,
  MousePointerClick,
  Hash,
  ClipboardList,
  Copy,
  Database,
  FolderOpen,
  AlertTriangle,
  Check,
  ShieldCheck,
  ShieldAlert,
  ShieldX,
  Shield,
  FileScan,
  Paperclip,
  FileText,
  Image,
  Globe,
  Search,
  Flame,
  Star,
  Circle,
  Mountain,
  Bug,
  Package,
  ExternalLink,
  Cloud,
  Wrench,
  Minimize2,
  Maximize2,
  X,
  Info,
  Ruler,
  Code,
  Plus,
  Eraser,
  FileBarChart,
  LineChart,
  Eye,
  EyeOff,
  ArrowDown,
  ArrowLeft,
  ArrowUp,
  Menu,
  Square,
  SendHorizontal,
  Loader2,
  Sparkles,
  Pencil,
  Trash2,
  RotateCcw,
  Zap,
  LogOut,
  Pin,
  Activity,
  FileSpreadsheet,
  ListPlus,
} from "lucide-react";

/** Provider 图标名称映射 */
export const PROVIDER_ICON_MAP: Record<string, LucideIcon> = {
  Globe,
  Search,
  Flame,
  Star,
  Circle,
  Mountain,
  Bug,
  Package,
  ExternalLink,
  Cloud,
  Wrench,
};

/** 根据 provider icon 字符串名获取图标组件 */
export const getProviderIcon = (
  iconName: string,
  size: number = 16,
  className?: string
): React.ReactNode => {
  const IconComponent = PROVIDER_ICON_MAP[iconName];
  if (IconComponent) {
    return <IconComponent size={size} className={className} />;
  }
  // fallback: 如果 icon 字段仍是 emoji（旧数据），直接显示
  if (iconName && /[\u{1F000}-\u{1FFFF}]/u.test(iconName)) {
    return <span style={{ fontSize: size }}>{iconName}</span>;
  }
  return <Wrench size={size} className={className} />;
};

// Re-export all icons for direct import
export {
  BarChart3,
  MessageSquare,
  Brain,
  Settings,
  User,
  Bot,
  Play,
  ChevronDown,
  ChevronLeft,
  ChevronRight,
  Clock,
  RefreshCw,
  CheckCircle,
  XCircle,
  BookOpen,
  PenLine,
  MousePointerClick,
  Hash,
  ClipboardList,
  Copy,
  Database,
  FolderOpen,
  AlertTriangle,
  Check,
  ShieldCheck,
  ShieldAlert,
  ShieldX,
  Shield,
  FileScan,
  Paperclip,
  FileText,
  Image,
  Globe,
  Search,
  Flame,
  Star,
  Circle,
  Mountain,
  Bug,
  Package,
  ExternalLink,
  Cloud,
  Wrench,
  Minimize2,
  Maximize2,
  X,
  Info,
  Ruler,
  Code,
  Plus,
  Eraser,
  FileBarChart,
  LineChart,
  Eye,
  EyeOff,
  ArrowDown,
  ArrowLeft,
  ArrowUp,
  Menu,
  Square,
  SendHorizontal,
  Loader2,
  Sparkles,
  Pencil,
  Trash2,
  RotateCcw,
  Zap,
  LogOut,
  Pin,
  Activity,
  FileSpreadsheet,
  ListPlus,
};

// Re-export LucideIcon type
export type { LucideIcon };
